[project]
name = "weather-agent-challenge"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
]

[tool.setuptools]
py-modules = ["weather_tool"]

[tool.pytest.ini_options]
testpaths = ["."]
